import { COLOR } from 'react-native-material-ui';

const uiTheme = {
    palette: {
      primaryColor: COLOR.blue500
    },
    toolbar: {
      container: {
        height: 50,
      },
    }
};

export default uiTheme;